# tests/fixtures —— 历史回放页升级的黄金样本

给 `tools/test_upgrade.js`（升级回归测试）用的五个代表世代的**真实回放页产物**。
样本从 git 历史里「复活」：逐个 checkout 当年的 `tools/svg2canvas.py`，用同一张小图
+ 小 SVG 重新跑出来 —— 不是手搓的假货，是真实世代形态（最新一代 s8 用工作区脚本生成）。

| 文件 | 来源 | 分工 |
|---|---|---|
| `s1-29770ea.html` | `29770ea` | 初版 —— 模糊规则全量（老时间轴 + 42 条布局升级） |
| `s4-eaa6ea5.html` | `eaa6ea5` | 中间世代 —— #18 的「世代跨度」场景 |
| `s6-v1.html` | `489f2a6` | v1（描述框固定高）—— 迁移链输入（v1→v2→v3 全链） |
| `s7-v2.html` | 快照（冻结） | v2（描述框自动变高）—— v2→v3 迁移的输入 |
| `s8-v3.html` | 工作区 | 当前模板 v3（描述框可输入）—— 直通验证 |

**为什么是这五个**：每个都有明确分工（见上表）。更细的世代样本（s2 缩放初版 /
s3 相册式初版 / s5 描述首版）在 2026-09-14 的精简里拿掉了 —— 它们能测的东西
s1/s4/s6 都已覆盖，而「样本数」本身就是维护注意力。**加样本前先想清楚：
它防的是哪次事故？**（s8 是模板升 v3 时为「直通验证」新增的：老 s7 于是退位为
v2→v3 迁移的输入。）

**重新生成**：`python3 tools/make_upgrade_fixtures.py`（缺谁补谁；`--force` 全部重来，
冻结样本除外 —— `s7` 是模板 v2 的快照，重造会变成新模板形态，脚本只跳过不覆盖）。
固定 commit 的样本（s1/s4/s6）重生成结果与这份**字节级一致**（2026-09-14 验证过）；
`s8` 跟随工作区（模板下次升级时它退位为输入、另加新样本）。
改完样本记得跑 `node tools/test_upgrade.js`。

**改断言前先读这条**：这些样本是「升级前」的形态，测试断言的是「升级后应达到的性质」
（版本戳、幂等、防重复哨兵、逐样本特性）。`s1` 是最老世代，升级后**没有**完整的缩放
交互（老 JS 里根本没有缩放系统，文本替换补不出整套函数）——这是规则集对最老世代的
已知覆盖边界，页面能播能看、零报错（「安全降级」）；且网页版历史里不可能有这代产物
（网页版 9-12 才上线），它主要用于压测最坏情况。逐样本特性的期望值写在
`tools/test_upgrade.js` 的 `FEATURES` 表里。
