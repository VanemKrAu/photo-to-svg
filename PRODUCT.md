# Product

## Register

product

## Users

想把照片变成矢量图的人：画手、设计师、做周边/印刷的人，以及想给作品配一段「手绘过程」视频的人。
他们通常在**晚上、室内、桌面前**使用，导入一张自己已经拍好的照片，然后等 1–3 分钟看它被一笔一笔画出来。
要办的事：拿到一个真能用的 SVG 文件，外加一段可以直接发给别人看的回放页。

## Product Purpose

在浏览器里把位图照片描摹成真正的矢量 SVG，并把整个作画过程做成可回放的网页 ——
先勾线稿、再铺大色块、最后抠细节，按「视觉重量」分配时间，像一段绘画录像。
全部本地完成（Pyodide 跑同一份 Python），图片不上传。

成功的样子：用户拿到能继续编辑的 SVG，并且回放**看起来像人手画的**，不像机器逐层渲染。

## Brand Personality

精致、克制、有手作感。

它是工具，不是玩具；但它服务的是「手作」这件事，所以不能冷冰冰地像一块企业后台。
界面应当安静、精确、有实体感 —— 像暗房里的一盏红灯：环境是暗的，光只落在正在显影的那张纸上。

## Anti-references

- **AI 深色 SaaS 模板**：纯黑底 + 霓虹描边、渐变文字（`background-clip:text`）、默认玻璃拟态、大圆角卡片 + 大面积模糊阴影
- **金色奢华风**（本项目原来的样子）：深紫黑 + 金色 accent，配上粉色蓝色渐变 logo —— 三种色系打架
- 每个 section 顶上一行小号大写字母的「eyebrow」，以及 01 / 02 / 03 式的编号装饰
- 装饰性动效：为动画而动画、页面载入的编排式入场

## Design Principles

1. **界面退到画面后面。** 用户是来看图被画出来的。画布区域的视觉权重永远高于周围的控件；任何 UI 都不该和它抢注意力。
2. **精致藏在细节里，不靠装饰堆。** 靠间距的节奏、层次的准确、状态反馈的及时 —— 而不是靠渐变、发光、贴纸式的效果。
3. **一个颜色只说一件事。** accent 只出现在「可以动手」的地方（主按钮、当前选中、键盘焦点）。它不做背景、不做边框装饰。
4. **等得起，但要看得见。** 生成要 1–3 分钟，进度、日志、状态必须始终可读 —— 等待期间用户不该怀疑它是不是卡死了。
5. **手机和电脑是同一件事。** 不是「做大屏然后适配小屏」，而是两端各自都要好用：手机上单手能完成选图 → 调参 → 生成。

## Accessibility & Inclusion

- 目标 WCAG **AA**：正文对比度 ≥ 4.5:1，大字号 ≥ 3:1。
  （现状不达标：`--dim` `#71718c` 在 `#07070a` 上只有约 **4.15:1**，本轮必须修掉。）
- 尊重 `prefers-reduced-motion`，所有动效提供减弱替代。
- 键盘可达：`Tab` 能走完选图 → 参数 → 生成，焦点环始终可见。
- 状态不能只靠颜色表达（错误、成功、进行中都要有文字或图标）。
